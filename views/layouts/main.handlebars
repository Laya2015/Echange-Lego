<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/menu.css">
    {{!-- <script type="module" src="/public/js/affiche_echange.js"></script> --}}
    <title>{{titre}}</title>

    {{#each styles}}
    <link rel="stylesheet" href="{{this}}">
    {{/each}}
    {{#each scripts}}
        <script type="module" src="{{this}}"></script>
    {{/each}}
</head>

<body>
    {{>header}}
    <main>
        {{{body}}}
    </main>
    {{>footer}}
</body>

</html>